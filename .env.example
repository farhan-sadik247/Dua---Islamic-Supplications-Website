# Frontend API URL (optional - defaults to /api for Vercel)
# For local development with separate backend server:
# NEXT_PUBLIC_API_URL=http://localhost:5000/api

# For Vercel deployment (uses Next.js API routes):
NEXT_PUBLIC_API_URL=/api

# Node Environment
NODE_ENV=production
